<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Welcome</title>
  </head>
  <body>
    <% if (user) { %>
    <h1>Hi <%= `${user.name[0].toUpperCase()}${user.name.slice(1)}` %>!</h1>
    <% } %>
    <h2>
      <a href="/movies">View Movies</a>
      - or -
      <a href="/movies/addmovie">Add Movie</a>
    </h2>
  </body>
</html>
